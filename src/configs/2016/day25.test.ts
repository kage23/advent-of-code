import { findAGoodNumber } from './day25'

describe('2016 Day 25', () => {
  describe('Part 1', () => {
    it('should find a good starting number', () => {
      expect(findAGoodNumber().answer1).toEqual(180)
    })
  })
})
