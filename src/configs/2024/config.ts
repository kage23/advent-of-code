import { YearConfig } from '../../routes/Year'
import cover from './cover.png'

const year2024: YearConfig = {
  cover,
  days: new Map(),
  extraText: 'I did it in Python this year, so you\'ll have to just clone the repo and run the .py files yourself!',
  id: 2024,
}

export default year2024
